import 'package:flutter/material.dart';

class WhoHomeScreen extends StatelessWidget {
  const WhoHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("TODO"));
  }
}
